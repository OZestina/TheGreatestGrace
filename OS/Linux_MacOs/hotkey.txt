구분이 필요한 경우가 아니면 띄어쓰기는 하지 않는다

#magic code
!#/bin/bash
!#/bin/sh

#스크립트 쉘에서 명령어 내용을 출력하고 싶은 경우
변수명=$(명령어)
echo "변수명"

find . -type f /( -name "*~" -o -name "&*&" /) -print -delete

Vimtutor 명령어로 vim 연습파일을 실행할 수 있다

220520 금
//시험...재미있었다!!
//strcpy문제를 너무 어렵게 생각해서 시간을 많이쓴게 아쉽지만, 결국 해결!
//atoi도 고민해봐야지
